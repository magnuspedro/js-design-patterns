const CreateOperatorStrategy = function() {
    this.create = (param) => `I work with the doctor and I'm pretty sure that he isn't the best doctor in Brazil, his ego is kind HUGE \n${param}`
}

module.exports = { CreateOperatorStrategy }